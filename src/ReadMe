Install Html package to your virtual environment. This package is different from the pre-installed html Python package.
Use "tar -xvzf Html-1.16.tar.gz" to unzip the file.

cgi.escape is no longer supported after Python 3.7

Replaced cgi.escape with html.escape
